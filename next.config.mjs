/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'img.freepik.com', 'i.pinimg.com', 'images-wixmp-ed30a86b8c4ca887773594c2.wixmp.com'],
    },
};

export default nextConfig;
